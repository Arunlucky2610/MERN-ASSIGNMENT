# MongoDB Connection String (use MongoDB Atlas for production)
# Render uses MONGO_URI by convention, but code supports both
MONGO_URI=mongodb+srv://username:password@cluster0.xxxxx.mongodb.net/event-platform
# MONGODB_URI also works (for local development)
# MONGODB_URI=mongodb://localhost:27017/event-platform

# JWT Secret Key (use a strong random string in production)
JWT_SECRET=your-super-secret-jwt-key-change-this

# JWT Expiration Time
JWT_EXPIRE=7d

# Server Port (Render provides this automatically)
PORT=5000

# Frontend URL for CORS (update with your Vercel/Netlify URL in production)
CLIENT_URL=http://localhost:3000

# Node Environment
NODE_ENV=development
